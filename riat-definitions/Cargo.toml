[package]
name = "riat-definitions"
description = "riat script compiler definition generator"
version = "0.2.0"
authors = ["Snowy Mouse"]
license = "GPL-3.0-only"

[lib]
proc-macro = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
